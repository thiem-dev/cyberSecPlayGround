

## troll in toolbox
- -i ignore case and * search all files for troll, -r recursive
`grep -r -i "troll" *` 

## present engine 
- in the workshop directory
`chmod 775 present_engine` then run it

## electrical blown fuse
`mv blown_fuse0 fuse0`

## symbolic link fuse
`ln -s ./fuse0 ./fuse1`